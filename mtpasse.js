var copyBtn  = document.getElementBy('copy')

function getPassword(length){
   const majuscules = "ABCEFGHIJKLMNOPQRSTUVWXYZ";
   const minuscules = "abcdefghijklmnopqrstuvwxyz";
   const chiffres = "123456789";
   const symboles = "!@#$%^&*()-_=+[]{}|;:,.<>?";
}
for (let i = 0; i <passwordLength ; i++){
     let ramdomNumber = Math.floor(Math.random * chars.length)
    }
    /const length = parseInt(document.getElementById('passwordLength').value, 10);
    if (length < 12 || length > 128) {
     alert('La longueur doit être comprise entre 12 et 128 caractères.');
    return;
}